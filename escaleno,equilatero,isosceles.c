
#include <stdio.h>
#include<math.h>

int main()
{
  float a,b,c;
  int triangulo = 0
  printf("digite o valor de a: ");
  scanf("%f",&a);
  printf("digite o valor de b: ");
  scanf("%f",&b);
  printf("digite o valor de c: ");
  scanf("%f",%c);
  if(a<(b+c)&& a>abs(b-c)){
    if(b<(a+c)&& b<abs(a-c)){
        if(c<(a+b)&& c<abs(a-b)){
            triangulo =1;
        }
    }
  }
  

    return 0;
}

